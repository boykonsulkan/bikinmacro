export default function Footer() {
  return (
    <footer className="w-full border-t border-t-border p-8 flex justify-center text-center text-xs text-muted">
      <p>
        BikinMacro &copy; {new Date().getFullYear()}. Dari kalimat jadi macro. Tanpa coding.
      </p>
    </footer>
  )
}
